server:
  port: 8081

spring:
  application:
    name: PATIENT-SERVICE

  datasource:
    url: jdbc:mysql://${HMS_DB_HOST:127.0.0.1}:${HMS_DB_PORT:3306}/patient_db?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: ${HMS_DB_USER:patient_user}
    password: ${HMS_DB_PASSWORD:patient_pass}
  flyway:
    clean-disabled: false
    clean-on-validation-error: true
    baseline-on-migrate: true
    baseline-version: 1
    validate-on-migrate: false
    hikari:
      initializationFailTimeout: -1
      connectionTimeout: 20000

  jpa:
    hibernate:
      ddl-auto: update # Changed from validate to fix schema mismatches
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        boot:
          allow_jdbc_metadata_access: false

eureka:
  instance:
    prefer-ip-address: true
    ip-address: 127.0.0.1
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
